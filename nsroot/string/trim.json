{
  "prefix": "string trim",
  "body": "${0:trimmed}=\\$(echo -e \"\\${${1:string}}\" |  sed -e 's/^[[:space:]]*//' | sed -e 's/[[:space:]]*\\$//')\n",
  "description": "remove leading and trailing white space(s)"
}
